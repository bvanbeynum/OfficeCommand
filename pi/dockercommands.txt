docker run -d \
  --name officecommand-pi \
  --network host \
  -e TZ="America/New_York" \
  --device=/dev/ttyACM0 \
  -v /home/bvanbeynum/officecommand/pi:/usr/src/app \
  --restart unless-stopped \
  officecommand-pi-image:latest
